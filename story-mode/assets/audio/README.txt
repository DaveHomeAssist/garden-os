Garden OS audio placeholders

This directory is the placeholder home for Phase 3A.

- `sfx/` documents one-shot effects expected by `AudioManager`
- `ambient/` documents seasonal and zone loops
- Real `.ogg` / `.mp3` files can replace these notes later without changing the runtime contract
