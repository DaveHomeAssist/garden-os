{
  "name": "garden-os-story-mode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/stamp-dist-build.mjs",
    "build:single": "npm run build && node scripts/export-single-file.mjs",
    "preview": "vite preview",
    "preview:dist": "node scripts/local-dist-transform-server.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "jsdom": "^20.0.3",
    "vite": "^3.2.8",
    "vitest": "^0.28.5"
  }
}
